/*
 * given two integers N representing an array size and K
 * representing the number of operation to carry out,
 * and an array of length N containing integers, minimize
 * the sum of the array with K iterations of the operation
 * floor(n/2) with updating arrays
 */

/// takes array arr and K number of operations
int operate(int arr[], int K) {
    return;
}

int main(int argc, char* argv[]) {
    int input(argv[1]);

}